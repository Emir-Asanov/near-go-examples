[package]
name = "yield-resume-integration-tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
near-workspaces = { version = "0.14", features = ["unstable"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
serde_json = "1"
